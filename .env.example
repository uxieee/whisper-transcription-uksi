# No env vars are required for browser-local transcription.
#
# Optional settings for the legacy local-Python API route:
# LOCAL_PYTHON_BIN=./venv/bin/python
# LOCAL_WHISPER_MODEL=turbo
# LOCAL_WHISPER_DEVICE=cpu
# MAX_UPLOAD_MB=64
# LOCAL_TRANSCRIBE_TIMEOUT_MS=900000
